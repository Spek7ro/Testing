>>> from calculadora import Calculadora
>>> calc = Calculadora()

>>> calc.sumar(2,3) 
5

>>> calc.sumar(20,30) 
50

>>> calc.sumar("X",30) 
'Solo se aceptan numeros'

>>> calc.sumar(-2,3)
'Solo numeros positivos'

>>> calc.sumar(1, 3.5)
'Solo numeros enteros'

>>> calc.sumar(4.6, 9.5)
'Solo numeros enteros'

>>> calc.sumar([4,5,6], 3.5)
'Solo acepto numeros no listas'

>>> calc.restar(10,5) 
5

>>> calc.restar(100,30) 
70

>>> calc.restar("X",30) 
'Solo se aceptan numeros'

>>> calc.restar(-2,3)
'Solo numeros positivos'

>>> calc.restar(1, 3.5)
'Solo numeros enteros'

>>> calc.restar(4.6, 9.5)
'Solo numeros enteros'

>>> calc.restar([4,5,6], 3.5)
'Solo acepto numeros no listas'


>>> calc.multiplicar(10,5) 
50

>>> calc.multiplicar("X",30) 
'Solo se aceptan numeros'

>>> calc.multiplicar(-2,3)
'Solo numeros positivos'

>>> calc.multiplicar(1, 3.5)
'Solo numeros enteros'

>>> calc.multiplicar(4.6, 9.5)
'Solo numeros enteros'

>>> calc.multiplicar([4,5,6], 3.5)
'Solo acepto numeros no listas'


>>> calc.dividir(10,5) 
2.0

>>> calc.dividir(50,0) 
'No se puede dividir entre cero'

>>> calc.dividir("X",30) 
'Solo se aceptan numeros'

>>> calc.dividir(-2,3)
'Solo numeros positivos'

>>> calc.dividir(1, 3.5)
'Solo numeros enteros'

>>> calc.dividir(4.6, 9.5)
'Solo numeros enteros'

>>> calc.dividir([4,5,6], 3.5)
'Solo acepto numeros no listas'


>>> calc.potencia(2,5) 
32

>>> calc.potencia(-2,2) 
'Solo numeros positivos para la base'

>>> calc.potencia("X",30) 
'Solo se aceptan numeros'

>>> calc.potencia([4,5,6], 3.5)
'Solo acepto numeros no listas'

>>> calc.potencia(3.6, 2)
'Solo numeros enteros para la base, enteros o flotantes para el exponente'
